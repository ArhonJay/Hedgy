{
  "name": "hedgy-smart-contracts",
  "version": "1.0.0",
  "description": "Smart contract system for Hedera Network with token, faucet, buy, and sell functionality",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network hederaTestnet",
    "interact:testnet": "hardhat run scripts/interact.js --network hederaTestnet",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "verify:testnet": "hardhat run scripts/verify.js --network hederaTestnet",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "coverage": "hardhat coverage"
  },
  "keywords": [
    "hedera",
    "blockchain",
    "smart-contracts",
    "solidity",
    "hardhat"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
